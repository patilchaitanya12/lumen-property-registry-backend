[project]
name = "lumen-property-registry-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "patilchaitanya12", email = "patilchaitanya9577@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
lumen-property-registry-backend = "lumen_property_registry_backend:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
